spring:
  # 应用名称
  application:
    name: AI-Qwen

  # 应用名称

  # 数据源配置
  datasource:
    url: jdbc:mysql://localhost:3306/ai_qwen?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: hjh421678
    driver-class-name: com.mysql.cj.jdbc.Driver
    # HikariCP 连接池配置
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      auto-commit: true
      idle-timeout: 30000
      pool-name: MyHikariCP
      max-lifetime: 1800000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  # JPA 配置
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: true

# ... existing code ...

server:
  port: 8080
  servlet:
    context-path: /AI-Qwen
  tomcat:
    uri-encoding: UTF-8

qwenApi:
  key: sk-5376f7a75c2e4f7ea983443538083204
  baseUrl: https://dashscope.aliyuncs.com/compatible-mode/v1
  model: deepseek-v3.2